#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    int x;
    cin >> x;

    int a,b;
    for(int i=0; i<x; i++){
        cin >> a >> b;
        cout << a+b << endl;
    }
    

    return 0;
}